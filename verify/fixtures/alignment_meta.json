{
  "n": 1000,
  "selection": "np.linspace(0, N-1, 1000).astype(int) over passages.db id",
  "dims": 384,
  "dtype": "float32",
  "stored_vector_norm_min": 0.9999998807907104,
  "stored_vector_norm_max": 1.0000001192092896,
  "gate": "AlignedEmbedder().embed(text) . stored_vector >= 0.98 for all rows",
  "note": "stored vectors are L2-normalised, so the dot product is cosine similarity directly"
}
